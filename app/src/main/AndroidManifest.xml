<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baadmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BAAdmin">
        <activity android:name=".Barbar"></activity>
        <activity android:name=".BrHome" />
        <activity android:name=".Movie" />
        <activity android:name=".MovHome" />
        <activity android:name=".Library" />
        <activity android:name=".LibHome" />
        <activity android:name=".Hospital" />
        <activity
            android:name=".HpHome"
            android:label="@string/title_activity_hp_home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>